import PageGenerator from '@/components/PageGenerator/PageGenerator'
import Button from '@/o-ui/Button/Button'
import Link from 'next/link'
import { HiMenuAlt2 } from 'react-icons/hi'

export const meta = {
    title: 'Button',
    description: '...',
    date: '-',
}

export const propTypes = [
    {
        property: 'size',
        type: ['sm', 'md', 'lg'],
        default: 'md',
        description: 'Size of the button',
    },
    {
        property: 'variant',
        type: ['primary', 'secondary', 'tertiary'],
        default: 'primary',
        description: 'Controls button appearance',
    },
    {
        property: 'loading',
        type: ['boolean'],
        default: false,
        description: 'Shows loading spinner',
    },
    {
        property: 'type',
        type: ['button', 'reset', 'submit'],
        default: 'button',
        description: 'Set the original html type of button',
    },
    {
        property: 'iconLeft',
        type: ['React.ReactElement'],
        default: '',
        description: '',
    },
    {
        property: 'iconRight',
        type: ['React.ReactElement'],
        default: '',
        description: '',
    },
    {
        property: 'disabled',
        type: ['boolean'],
        default: false,
        description: 'Makes button disabled',
    },
    {
        property: 'className',
        type: ['string'],
        default: '',
        description: '',
    },
    {
        property: 'children',
        type: ['React.ReactNode'],
        default: '',
        description: '',
    },
    {
        property: 'onClick',
        type: ['React.MouseEventHandler'],
        default: '',
        description: '',
    },
]

export default ({ children }) => (
    <PageGenerator meta={meta} propTypes={propTypes}>
        {children}
    </PageGenerator>
)

---

### Variants

<div className="flex gap-2">
    <Button variant="primary">Button</Button>
    <Button variant="secondary">Button</Button>
    <Button variant="tertiary">Button</Button>
</div>

```jsx
<div className="flex gap-2">
    <Button variant="primary">Button</Button>
    <Button variant="secondary">Button</Button>
    <Button variant="tertiary">Button</Button>
</div>
```

---

### Sizes

<div className="flex gap-2">
    <Button size="sm">Button</Button>
    <Button size="md">Button</Button>
    <Button size="lg">Button</Button>
</div>

```jsx
<div className="flex gap-2">
    <Button size="sm">Button</Button>
    <Button size="md">Button</Button>
    <Button size="lg">Button</Button>
</div>
```

---

### With icons

<div className="flex gap-2">
    <Button iconLeft={<HiMenuAlt2 className="mr-1 h-4 w-4" />}>Button</Button>
    <Button iconRight={<HiMenuAlt2 className="ml-1 h-4 w-4" />}>Button</Button>
</div>
```jsx
<div className="flex gap-2">
    <Button iconLeft={<HiMenuAlt2 className="mr-1 h-4 w-4" />}>Button</Button>
    <Button iconRight={<HiMenuAlt2 className="ml-1 h-4 w-4" />}>Button</Button>
</div>
```

---

### Loading state

<div className="flex gap-2">
    <Button variant="primary" loading>
        Button
    </Button>
    <Button variant="secondary" loading>
        Button
    </Button>
    <Button variant="tertiary" loading>
        Button
    </Button>
</div>

```jsx
<div className="flex gap-2">
    <Button variant="primary" loading>
        Button
    </Button>
    <Button variant="secondary" loading>
        Button
    </Button>
    <Button variant="tertiary" loading>
        Button
    </Button>
</div>
```

---

### Disabled

<div className="flex gap-2">
    <Button variant="primary" disabled>
        Button
    </Button>
    <Button variant="secondary" disabled>
        Button
    </Button>
    <Button variant="tertiary" disabled>
        Button
    </Button>
</div>

```jsx
<div className="flex gap-2">
    <Button variant="primary" disabled>
        Button
    </Button>
    <Button variant="secondary" disabled>
        Button
    </Button>
    <Button variant="tertiary" disabled>
        Button
    </Button>
</div>
```
