import Link from "next/link"
import { useState } from 'react'
import { HiMenuAlt2 } from 'react-icons/hi'
import PageGenerator from "@/components/PageGenerator/PageGenerator"
import DropDown from '@/o-ui/DropDown/DropDown'
import Modal from '@/o-ui/Modal/Modal'
import Button from '@/o-ui/Button/Button'

export const meta = {
  title: 'Modal',
  description: '...',
  date: '-',
}

export const propTypes = [
  {
    property: "button",
    type: ["React.ReactElement"],
    default: "",
    description: "Button to open Dropdown",
  },
  {
    property: "direction",
    type: ["left","right"],
    default: "",
    description: "Defines direction of Dropdown",
  },
  {
    property: "children",
    type: ["children"],
    default: "false",
    description: "...",
  },
];


export default ({ children }) => <PageGenerator meta={meta} propTypes={propTypes}>{children}</PageGenerator>;


{/* -------------------------------------------------------------------------------------- */}


export const Example = () => {
  return (
   <DropDown button={<Button>MENU</Button>} direction={'left'}>
        <DropDown.Item>Home</DropDown.Item>
        <DropDown.Item>Profile</DropDown.Item>
        <DropDown.Divider />
        <DropDown.Item>Settings</DropDown.Item>
        <DropDown.Item>Logout</DropDown.Item>
    </DropDown>
  )
}

### Basic usage
<div className="flex gap-2">
<Example/>
</div>
```jsx
<div>
    <DropDown button={<Button>MENU</Button>} direction={'right'}>
        <DropDown.Item>Home</DropDown.Item>
        <DropDown.Item>Profile</DropDown.Item>
        <DropDown.Divider />
        <DropDown.Item>Settings</DropDown.Item>
        <DropDown.Item>Logout</DropDown.Item>
    </DropDown>
</div>
```
